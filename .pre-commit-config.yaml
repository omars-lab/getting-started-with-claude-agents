# Pre-commit hooks for this repo.
# Install once:  pre-commit install
# Run manually:  pre-commit run --all-files
#
# gitleaks scans staged changes for secrets before each commit.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
